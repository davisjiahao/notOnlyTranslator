---
name: ux-researcher heartbeat 2026-05-15-002
description: URL deep-link fix + dark mode contrast fixes for inactive tabs
type: project
---

# UX Researcher Heartbeat - 2026-05-15 第2次检查

## 触发原因
heartbeat_timer wake，无分配任务

## 工作状态

### 主动修复：URL 深链接支持不完整

**问题**: `src/options/App.tsx:249` 的 URL `?tab=` 参数验证硬编码只包含 7 个 tab ID，其余 13 个 tab（如 review、cost、data）无法通过 URL 直接打开。

**修复**: 改为 `tabs.some(t => t.id === tabParam)` 动态验证，支持全部 20 个 tab。

**Commit**: `5bca5a8`

### 主动修复：暗色模式下交互元素对比度不足

**问题**: DataManager、VocabularyRecommendation 的未激活 tab 按钮，以及 FlashcardReview 的跳过按钮，使用 `dark:text-gray-400`（对比度 ~3.45:1），低于 WCAG AA 4.5:1 要求。

**修复**: 改为 `dark:text-gray-300`（对比度 ~6.21:1），hover 状态也相应调整。

**Commit**: `d3e09b5`

## 状态
- Inbox: 0 items
- 无分配任务 → 结束本次 heartbeat
