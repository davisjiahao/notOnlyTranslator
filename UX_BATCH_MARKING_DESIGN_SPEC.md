# UX 设计规格 — F7.2 批量标记功能 (Batch Vocabulary Operations)

**日期:** 2026-05-18
**来源:** Nielsen 启发式评估发现 F7.2
**严重度:** P2 (低 → 中)
**范围:** Options 页面 VocabularySettings + Popup VocabularyList

---

## 问题描述

用户无法批量将多个生词标记为已掌握或从生词本移除。当前生词本管理只能逐个删除/标记，对于积累了数十甚至上百个生词的用户，逐一操作效率极低。

## 用户场景

### 场景 1: 复习后批量清理
用户在阅读文章时标记了大量词汇，经过一段时间学习后已经认识了其中大部分，想一次性将它们从生词本移除。

### 场景 2: 筛选后批量操作
用户搜索"apple"后，想将与该词相关的所有衍生词（apples, apple's）一起标记。

### 场景 3: 全选清空
用户觉得当前生词本质量不高，想清空重新开始，但不想一个一个删。

---

## 设计方案

### 核心交互模式：选择 → 操作

采用"进入选择模式 → 选中项目 → 执行操作"的三段式交互，与移动端相册批量删除的常见模式一致。

### 1. 选择模式进入/退出

| 触发方式 | 说明 |
|---------|------|
| **进入** | 点击词汇列表上方的"选择"按钮，或长按某个词条 0.5s |
| **退出** | 点击"取消选择"按钮，或所有选中项被操作后自动退出 |
| **视觉反馈** | 进入选择模式后，每个词条左侧出现复选框 |

### 2. 选择方式

| 操作 | 效果 |
|------|------|
| 点击复选框 | 选中/取消该条目 |
| 点击"全选" | 选中当前筛选结果中的所有词条 |
| 长按词条 | 进入选择模式并自动选中该词条（移动端友好） |
| Shift+点击 | 区间选择（桌面端） |

### 3. 批量操作栏

进入选择模式后，底部固定操作栏显示：

```
[✓ 已选 3 个]           [标为已掌握] [批量删除] [取消选择]
```

**操作说明：**
- **标为已掌握**: 将选中词条移入 knownWords 列表（等同逐个点"认识"）
- **批量删除**: 从生词本移除选中词条（有 3s 撤销窗口）
- **取消选择**: 退出选择模式

### 4. 撤销机制

批量操作后显示撤销提示，与单个删除一致：

```
已处理 3 个词条 ✓ [撤销]
```

3 秒后自动确认操作。

---

## UI 组件设计

### VocabularySettings (Options 页面) — 主要实现目标

**当前状态（修改前）：**
```
┌────────────────────────────────────┐
│ 搜索框 [_____]  排序 [最近添加 ▼] │
├────────────────────────────────────┤
│ apple   苹果          🗑️            │
│ banana  香蕉          🗑️            │
│ cherry  樱桃          🗑️            │
├────────────────────────────────────┤
│ 共 3 个词汇          [清空生词本]   │
└────────────────────────────────────┘
```

**修改后：**
```
┌────────────────────────────────────┐
│ 搜索框 [_____]  排序 [最近添加 ▼] │
├────────────────────────────────────┤
│ [选择] 筛选: 全部 ▼  按词频 ▼      │
├────────────────────────────────────┤
│ ☐ apple   苹果        🗑️            │
│ ☐ banana  香蕉        🗑️            │
│ ☐ cherry  樱桃        🗑️            │
├────────────────────────────────────┤
│ 共 3 个词汇          [清空生词本]   │
└────────────────────────────────────┘
```

**选择模式激活后：**
```
┌────────────────────────────────────┐
│ 搜索框 [_____]  排序 [最近添加 ▼] │
├────────────────────────────────────┤
│ [取消]  [全选]                     │
├────────────────────────────────────┤
│ ☑ apple   苹果                     │
│ ☑ banana  香蕉                     │
│ ☐ cherry  樱桃                     │
├────────────────────────────────────┤
│ ✓ 已选 2 个        [批量删除] [标为已掌握] │
└────────────────────────────────────┘
```

### VocabularyList (Popup) — 简化版本

Popup 空间有限（360px 宽），采用更轻量的交互：

- 长按词条进入选择模式
- 仅支持"批量删除"操作
- 操作后 toast 提示确认

---

## 无障碍设计 (Accessibility)

| 要求 | 实现 |
|------|------|
| 选择模式可感知 | 进入选择模式时 `aria-live="polite"` 通知："已进入选择模式" |
| 复选框标签 | 每个复选框 `aria-label="选择 {word}"` |
| 计数通知 | 选中数量变化时 `aria-live="assertive"`："已选择 2 个词条" |
| 键盘导航 | Tab 键遍历复选框，Space 切换选中状态 |
| 操作确认 | 批量操作后 `role="status"` 通知操作结果 |

---

## 技术要点

### 数据流

```
用户选择多个词条
    ↓
前端收集选中词条列表: string[]
    ↓
发送批量消息: BATCH_UPDATE_VOCABULARY
  payload: { words: string[], action: 'remove' | 'markKnown' }
    ↓
Background service worker 处理
    ↓
返回结果: { success: true, processedCount: number }
    ↓
前端更新 UI + 显示撤销提示
```

### 需要新增的消息类型

在 `src/shared/types/index.ts` 中新增：
```typescript
{
  type: 'BATCH_UPDATE_VOCABULARY',
  payload: {
    words: string[],
    action: 'remove' | 'markKnown' | 'markUnknown',
  }
}
```

### 需要修改的文件

| 文件 | 变更 |
|------|------|
| `src/shared/types/index.ts` | 新增 BATCH_UPDATE_VOCABULARY 消息类型 |
| `src/background/index.ts` | 新增消息处理路由 |
| `src/options/components/VocabularySettings.tsx` | 新增选择模式 UI + 批量操作逻辑 |
| `src/popup/components/VocabularyList.tsx` | 新增选择模式 UI（简化版） |
| `src/shared/components/EmptyState.tsx` | 无需修改 |

### 性能考虑

- 批量操作应在后台逐条处理，每词条间 50ms 延迟避免 UI 卡顿
- 超过 100 个词条时显示进度指示器
- 操作期间禁用其他交互按钮

---

## 验收标准

- [ ] 用户能进入/退出选择模式
- [ ] 用户能选择单个/多个/全部词条
- [ ] 批量删除后显示撤销提示（3s 窗口）
- [ ] 批量标记已知后词条从生词本移除
- [ ] 键盘可完成全部选择操作（Tab + Space）
- [ ] 屏幕阅读器能播报选择状态和操作结果
- [ ] 操作期间有明确的进行中状态（loading/disabled）
- [ ] 筛选/排序后选择不影响未显示词条

---

## 与现有 UX 模式的对比

| 现有模式 | 批量操作 |
|---------|---------|
| 单个删除有 2s 撤销窗口 ✅ | 批量删除复用相同撤销机制 ✅ |
| Toast 通知基于内容长度 ✅ | 操作完成显示"已处理 N 个词条" ✅ |
| 空状态统一 EmptyState ✅ | 筛选为空时复用 EmptyState ✅ |

---

## 建议实现顺序

1. **Phase 1**: VocabularySettings 添加选择模式 + 批量删除（最高频场景）
2. **Phase 2**: 添加批量标为已掌握
3. **Phase 3**: Popup VocabularyList 添加简化版选择模式
4. **Phase 4**: 键盘快捷键增强（Shift+区间选择）

Phase 1 即可覆盖 80% 的批量化需求。
